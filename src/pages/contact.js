import React from 'react'
import Link from 'gatsby-link'
import { GreyHeader } from '../components/headers'

const MediaPage = () => (
  <div>
    <GreyHeader title="Contact Us" subtitle="Don't be shy, we don't bite." />
  </div>
)

export default MediaPage
